@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_SettingLayout.cshtml";
}

<div id="page_content_inner">
    <br />
    <h4 class="heading_a uk-margin-bottom">
        <div class="titletext">
            <h1 class="page-title">
                Business List
                <small>View businesses or create a new one.</small>
            </h1>
            </div>


            <div class="clsDivion hide">
                Division List
                <p>View division or create a new one for business.</p>
            </div>
            <div class="clsPool hide">
                Pool List
                <p>View pools or create a new one.</p>
            </div>
            <div class="clsFunction hide">
                Function List
                <p>View functions or create a new one.</p>
            </div>
         

        <div class="clearfix"></div>
    </h4>
    <button class="btn btn-primary btn-businessList">BUSINESS</button>
    <button class="btn btn-divisionList">DIVISION</button>
    <button class="btn btn-poolList">POOL</button>
    <button class="btn btn-functionList">Function</button>
    <br />
    <br />
    <div class="md-card">
        <div >
            <div id="tableDiv">
                @Html.Action("businessList", "CompanyStructure")
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">
    var constant = {
        BizList: '@Url.Action("businessList", "CompanyStructure")',
        DivList: '@Url.Action("divisionList", "CompanyStructure")',
        PolList: '@Url.Action("poolList", "CompanyStructure")',
        FunList: '@Url.Action("functionList", "CompanyStructure")',
        AddBiz: '@Url.Action("addBusinessList", "CompanyStructure")',
        AddDiv: '@Url.Action("addDivisionList", "CompanyStructure")',
        AddPol: '@Url.Action("addPoolList", "CompanyStructure")',
        AddFun: '@Url.Action("addFunctionList", "CompanyStructure")',
        bindDiv: '@Url.Action("bindDivisionList", "CompanyStructure")',
        EditBiz: '@Url.Action("editBusinessName", "CompanyStructure")',
        DeleteBiz: '@Url.Action("deleteBusiness", "CompanyStructure")',
        EditDiv: '@Url.Action("editDivisionName", "CompanyStructure")',
        DeleteDiv: '@Url.Action("deleteDivision", "CompanyStructure")',
        EditPol: '@Url.Action("editPoolName", "CompanyStructure")',
        DeletePol: '@Url.Action("deletePool", "CompanyStructure")',
        EditFun: '@Url.Action("editFunctionName", "CompanyStructure")',
        DeleteFun: '@Url.Action("deleteFunction", "CompanyStructure")'
    };

</script>

<script src="~/JS/Settings/CompanyStructure/CompanyStuctureJS.js"></script>